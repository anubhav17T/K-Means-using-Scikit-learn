# K-Means-using-Scikit-learn
In Scikit learn, clustering algorithm we use in-built function to perform the algorithm in the backend. Also we have initialised the Elbow method for the no of cluster in the given dataset.
